minitron*
engine*
.claude
.DS_Store
__pycache__/
*.pyc
figures/
artifacts/
.venv/

# Paper kept local only
paper/

# Secrets
.env

# All data stays local. Keep only the already-tracked spot store, metadata,
# and the small universe list; ignore everything else under data/ (L2, trades,
# raw caches, generated microstructure parquet, etc.)
data/*
!data/spot_1h
!data/metadata
!data/l2_universe_top50.txt

# local run logs
*_run.log

# LaTeX build artifacts (paper builds at repo root)
main.aux
main.log
main.out
main.toc
main.synctex.gz
main_compile.log
