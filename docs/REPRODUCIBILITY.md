# Reproducibility map

Every headline finding in `paper/main.tex`, mapped to the committed data file and the committed
script that produces it. Verified by a full re-audit (2026-06-19): the fast seeded scripts were
re-run and reproduced their committed outputs byte-for-byte; the slow L2/backtest scripts read
committed inputs and their committed outputs match the paper. "Re-run" = regenerated in the audit
and confirmed identical. "Committed output matches" = script + input present, output on disk equals
the paper (not re-run live because it is slow).

## Figures

| Figure | Finding | Data file | Script | Status |
|---|---|---|---|---|
| fig:kalman-innovations | Kalman whitens its own innovations | `scratch/cache_hourly/*.parquet` + live fit | `make_figures_v2.py` (uses `src/kalman_hedge.py`) | re-run |
| fig:poscontrol | screen passes pos+neg control alike | `scratch/kalman_positive_control.csv` | `kalman_positive_control.py` (seed 11) | **re-run, byte-identical** |
| fig:freq | placebo gap ~0 (Kalman) vs EG | `scratch/timeframe_kalman_artifact.csv` | `timeframe_robustness.py` (seed 7) | committed output matches |
| fig:rev-persist | OU speed predicts OOS reversion, rho=0.46 | `scratch/persistence_pairs.csv` | `persistence_test.py` / `persistence_robust.py` | **re-run, rho=0.460** |
| fig:ofi-decay | book-OFI R^2 decays to a noise floor by 30-300s | `scratch/ofi_decay_extended.csv` | `book_ofi_2024.py` via `run_ofi_extended.py` (reads `data/l2_raw/binance/`) | re-run; overlapping horizons reproduce `book_ofi_2024.log` exactly |
| fig:cancel-share | 77-90% of best-level withdrawals are cancels | `scratch/book_ofi_2024.log` table [3] | `book_ofi_2024.py` | committed output matches |
| fig:hac-inflation | IID/HAC inflation ~2x over 27 splits | `docs/hac_sharpe_per_split.csv` | `make_figures_v2.py` (n=27, median 2.0 confirmed) | committed output matches |
| fig:microcap-adv | held-out microcaps trade tens-to-thousands $/day | `scratch/microcap_adv.csv` | `microcap_adv.py` | **re-run, identical** |
| fig:forced-collapse | no-stop loses -117% to -258% per real collapse | `scratch/forced_collapse.csv` | `forced_collapse.py` | **re-run, identical** |
| fig:circuit-breaker | breaker keeps Sharpe positive; no-breaker crosses 0 near 50%/yr | `scratch/survivorship_adjusted_sharpe.csv` | `survivorship_adjusted_sharpe.py` | **re-run, identical (zero-cross 50.1%)** |
| fig:rollingz | rolling z reverts on a pure random walk | generated in-script (seed 3) | `make_figures.py` | re-run |

All figures are (re)generated by `python scratch/make_figures_v2.py` (plus `make_figures.py` for the
rolling-z noise panel) from the CSV/log files above.

## Tables

| Table | Source | Script | Status |
|---|---|---|---|
| tab:kalman-placebo | `scratch/part1.log`, `part1b.log`, `docs/CORRECTION_kalman_cointegration.md` | `audit_part1.py`, `audit_part1b.py` (seeded) | committed output matches |
| tab:poscontrol | `scratch/kalman_positive_control.csv` | `kalman_positive_control.py` | **re-run, byte-identical** |
| tab:kalman-freq | `scratch/timeframe_kalman_artifact.csv` | `timeframe_robustness.py` | committed output matches |
| tab:rollingz | `scratch/timeframe_rollingz_floor.csv` | `timeframe_robustness.py` | committed output matches |
| tab:institutional-impact | `scratch/impact_decomp_2024.log` (R_c(h) RAW, h=1) | `impact_decomp_2024.py` | committed output matches |
| tab:exec | `scratch/exec_value_2024.log` (pooled, 29,952 orders) | `exec_value_2024.py` | committed output matches |
| tab:exec-symbol-breakdown | `scratch/exec_value_2024_summary.csv` (`agg`, `l3_30` cols) | `exec_value_2024.py` | committed output matches |
| tab:venue-comparison (T1) | `scratch/independent_verify.log` (Binance 2.18), `cross_exchange.log` (Coinbase 0.88) | `independent_verify.py`, `cross_exchange.py` | re-run: Binance naive 2.18, monthly HAC 1.3-1.5 (lag 3-6, midpoint ~1.4) |

## Method definitions

Every method/metric in the body is defined in Appendix A (`\section{Methods background}`), now
cross-referenced from the body at first use. The appendix formulas (Kalman recursion, ADF,
Engle-Granger, Newey-West HAC, deflated Sharpe, Ornstein-Uhlenbeck, walk-forward, the four placebos,
the microstructure measures, block bootstrap) were verified against the implementing code and are
correct. The block-bootstrap subsection describes the circular variant the code implements.

## Re-run from scratch

- Figures/tables from committed CSVs/logs: `python scratch/make_figures_v2.py`.
- Kalman controls: `python scratch/kalman_positive_control.py` (seeded, ~1-2 min).
- Reversion persistence: `python scratch/persistence_test.py` (then `persistence_robust.py`).
- Survivorship: `python scratch/survivorship_adjusted_sharpe.py`, `scratch/forced_collapse.py`, `scratch/microcap_adv.py`.
- L2 microstructure (needs `data/l2_raw/binance/`, 7 dates x 4 symbols, present): `python scratch/book_ofi_2024.py`, `impact_decomp_2024.py`, `exec_value_2024.py`, `hidden_liquidity_2024.py`, `inst_flow_2024.py`. Extended OFI horizons: `python scratch/run_ofi_extended.py`.
- Venue/HAC Sharpe: `python scratch/independent_verify.py`, `scratch/cross_exchange.py`.

## Previously writeup-only numbers, now reproduced with committed logs

These were recorded only in the `docs/L2_FINDINGS.md` writeup; they have since been re-run and the
generating output is committed, so each is now one-command reproducible:

- **median hold ~35 days and 78% non-convergence** -> `python scratch/wf_nostop_stress.py`
  writes `scratch/wf_nostop_stress.log`: median holding time 848 bars (= 35.3 days) and
  148/190 = 77.9% of positions still open (never converged) at window end.
- **four independent-reimplementation Sharpes 1.95 / 2.15 / 2.16 / 2.18** ->
  `scratch/independent_verify.log` (2.18) and `scratch/reimpl_runs.log` (1.95, 2.15, 2.16 from
  `indep3_backtest.py`, `indep_run2.py`, `my_independent_run1.py`), all written from scratch off the
  raw parquet, none finding look-ahead.
- **17 to 23% L2 cost saving** -> `python scratch/provenance_costsaving.py` from the committed
  `docs/l2_results/portfolio_flat_costs.csv` vs `portfolio_l2_costs.csv`: zscore 23.1%, xgboost 17.4%.
- **~1/3 generic survivor comovement floor** is the random-pair placebo no-stop Sharpe (+0.89)
  over the total no-stop Sharpe (+2.51), recorded in `docs/L2_FINDINGS.md:219-221`. This single
  ratio is still sourced from the writeup; the components are consistent with the reimplementation
  runs but a dedicated random-pair placebo log is not separately committed.

## Universe-wide L2 robustness (supports `main_proposed.tex`, not the canonical paper)

The proposed paper extends the three Section 5 L2 tests from the four event-level majors to the full
top-50 universe, over the same regime-spanning 2024 days (7 days for OFI, 13 for execution/impact).
The four majors reproduce the committed four-symbol numbers as a correctness check, so the broadened
numbers sit on the same footing as the paper's.

| Result | Script | Per-symbol data | 4-major reproduction |
|---|---|---|---|
| book-OFI decay across 50 | `scratch/book_ofi_broad.py` | `scratch/ofi_broad_50sym.csv` | contemporaneous incr book-OFI matches `book_ofi_2024.log` byte-for-byte |
| execution null across 50 | `scratch/book_exec_broad.py` | `scratch/exec_broad_50sym.csv` | AGG/L3 costs match `exec_value_2024_summary.csv` exactly |
| size-to-impact across 50 | `scratch/book_impact_broad.py` | `scratch/impact_broad_50sym.csv` | retail/mid/inst 1s impact matches Table 5 exactly |

Headline: the OFI-decay and execution nulls hold across all 50 (1s-ahead forecast R^2 < 0.5% for
every symbol; the signal-timed execution rule is costlier than crossing in 50/50). Size-to-impact
holds on the liquid names (39/50, all of the top 10) but turns noisy on the thinnest, so the proposed
paper keeps Table 5 to four majors. The breadth additions were adversarially verified (numbers,
overclaim, voice, consistency) before being written into `main_proposed.tex` via the deterministic
`scratch/apply_proposed*.py` transforms. The 114 MB intermediate `scratch/exec_broad_orders.csv` is
gitignored; `book_exec_broad.py` rebuilds or reuses it.
